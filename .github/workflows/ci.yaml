name: CI Pipeline

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
  push:
    branches: [main]
  # Allow this workflow to be called by other workflows
  workflow_run:
    workflows: ["Fast Checks & Auto-fix"]
    types: [completed]

# Cancel in-progress runs when new commits are pushed
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Wait for fast-checks to complete (only for PRs)
  wait-for-fast-checks:
    name: Wait for Fast Checks
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Wait for fast-checks workflow
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const ref = context.payload.pull_request.head.sha;
            
            console.log(`Waiting for fast-checks workflow on commit ${ref}`);
            
            // Wait up to 10 minutes for fast-checks to complete
            const maxWaitTime = 10 * 60 * 1000; // 10 minutes
            const pollInterval = 10000; // 10 seconds
            const startTime = Date.now();
            
            while (Date.now() - startTime < maxWaitTime) {
              // Get all workflow runs for this commit
              const { data: workflowRuns } = await github.rest.actions.listWorkflowRunsForRepo({
                owner,
                repo,
                head_sha: ref,
                per_page: 100
              });
              
              // Find the fast-checks workflow
              const fastChecksRun = workflowRuns.workflow_runs.find(
                run => run.name === 'Fast Checks & Auto-fix'
              );
              
              if (fastChecksRun) {
                console.log(`Fast-checks status: ${fastChecksRun.status} (${fastChecksRun.conclusion})`);
                
                if (fastChecksRun.status === 'completed') {
                  if (fastChecksRun.conclusion === 'success') {
                    console.log('✅ Fast-checks completed successfully');
                    return;
                  } else if (fastChecksRun.conclusion === 'failure') {
                    console.log('❌ Fast-checks failed - continuing with CI anyway');
                    return;
                  } else {
                    console.log(`⚠️ Fast-checks completed with status: ${fastChecksRun.conclusion}`);
                    return;
                  }
                }
              }
              
              // Wait before polling again
              await new Promise(resolve => setTimeout(resolve, pollInterval));
            }
            
            console.log('⏱️ Timeout waiting for fast-checks - continuing with CI');

      - name: Check for new commits
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const prNumber = context.payload.pull_request.number;
            
            // Get the latest PR data
            const { data: pr } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: prNumber
            });
            
            const currentSha = context.payload.pull_request.head.sha;
            const latestSha = pr.head.sha;
            
            if (currentSha !== latestSha) {
              console.log(`New commits detected. Current: ${currentSha}, Latest: ${latestSha}`);
              console.log('This workflow run is outdated and will be cancelled by concurrency group');
              core.setFailed('Outdated - new commits pushed');
            } else {
              console.log('No new commits - proceeding with CI');
            }

  pre-commit:
    name: Pre-commit Checks
    runs-on: ubuntu-latest
    needs: [wait-for-fast-checks]
    if: |
      always() && 
      (github.event_name == 'push' || 
       (github.event_name == 'pull_request' && needs.wait-for-fast-checks.result != 'failure'))
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.13"
          cache: 'pip'

      - name: Cache pre-commit
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Install pre-commit
        run: python -m pip install pre-commit

      - name: Configure pre-commit to skip workflow files
        run: |
          # Add SKIP environment variable to exclude files in .github/workflows/
          # This prevents permission errors when trying to modify workflow files
          echo "SKIP=gitleaks" >> $GITHUB_ENV
          export PRE_COMMIT_EXCLUDE='^\.github/workflows/.*$'
          echo "PRE_COMMIT_EXCLUDE=^\.github/workflows/.*$" >> $GITHUB_ENV
          echo "Pre-commit configured to skip .github/workflows/ directory"

      - name: Run pre-commit (first attempt)
        id: precommit_first
        continue-on-error: true
        env:
          SKIP: gitleaks
        run: |
          echo "Running pre-commit on all files (excluding .github/workflows/)..."
          pre-commit run --all-files --show-diff-on-failure

      - name: Check for changes after first run
        id: changes_first
        if: steps.precommit_first.outcome == 'failure'
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No file changes detected after pre-commit"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "File changes detected after pre-commit:"
            git diff --stat
          fi

      - name: Commit changes locally (not pushed yet)
        id: local_commit
        if: steps.precommit_first.outcome == 'failure' && steps.changes_first.outputs.changed == 'true'
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: auto-fix pre-commit issues"
          echo "Local commit created, not pushed yet"

      - name: Run pre-commit (second attempt - verification)
        id: precommit_second
        if: steps.precommit_first.outcome == 'failure' && steps.changes_first.outputs.changed == 'true'
        continue-on-error: true
        env:
          SKIP: gitleaks
        run: |
          echo "Re-running pre-commit to verify fixes..."
          pre-commit run --all-files --show-diff-on-failure

      - name: Check if second run made additional changes
        id: changes_second
        if: steps.precommit_second.outcome == 'failure'
        run: |
          if git diff --quiet; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "No additional changes after second pre-commit run"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Additional changes detected - this means pre-commit is still failing"
            git diff --stat
          fi

      # Case 1: Pre-commit fixed issues successfully (second run passed)
      - name: Push auto-fixes to PR
        if: |
          github.event_name == 'pull_request' && 
          steps.precommit_first.outcome == 'failure' && 
          steps.changes_first.outputs.changed == 'true' && 
          steps.precommit_second.outcome == 'success'
        run: |
          echo "Pre-commit fixes verified successfully, pushing to PR..."
          git push origin HEAD:${{ github.head_ref }}

      - name: Trigger workflow restart after successful auto-fix
        if: |
          github.event_name == 'pull_request' && 
          steps.precommit_first.outcome == 'failure' && 
          steps.changes_first.outputs.changed == 'true' && 
          steps.precommit_second.outcome == 'success'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // Wait for push to be processed
            await new Promise(resolve => setTimeout(resolve, 3000));
            
            // The push itself will trigger the workflow via 'synchronize' event
            console.log('Auto-fix pushed successfully. Workflow will restart automatically.');

      - name: Notify user about successful auto-fix
        if: |
          github.event_name == 'pull_request' && 
          steps.precommit_first.outcome == 'failure' && 
          steps.changes_first.outputs.changed == 'true' && 
          steps.precommit_second.outcome == 'success'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ✅ Pre-commit Auto-fix Applied

            **Pre-commit hooks have automatically fixed issues.**

            ### What happened?
            - 🔧 Pre-commit hooks ran and fixed auto-fixable issues
            - ✅ Changes have been verified and pushed to your PR branch
            - 🔄 CI checks are restarting automatically

            ### What you need to do:
            Please pull the latest changes to your local branch:
            ~~~bash
            git pull --rebase
            ~~~

            ### Next steps:
            - Your PR has been updated with the fixes
            - The CI pipeline will restart with the fixed code
            - All checks should pass now ✨`
            })

      # Case 2: Pre-commit made changes but still failing (or no changes but failing)
      - name: Notify user about manual fix needed
        if: |
          github.event_name == 'pull_request' && 
          steps.precommit_first.outcome == 'failure' && 
          (steps.changes_first.outputs.changed == 'false' || 
           (steps.precommit_second.outcome == 'failure' && steps.changes_second.outputs.changed == 'false'))
        uses: actions/github-script@v7
        with:
          script: |
            const madeChanges = '${{ steps.changes_first.outputs.changed }}' === 'true';
            const stillFailing = '${{ steps.precommit_second.outcome }}' === 'failure';
            
            let explanation = '';
            if (madeChanges && stillFailing) {
              explanation = '**Pre-commit made some fixes, but is still failing without making additional changes.**\nThis means there are issues that cannot be auto-fixed.';
            } else {
              explanation = '**Pre-commit found issues but could not auto-fix them.**';
            }
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ⚠️ Pre-commit Checks Failed - Manual Fix Required

            ${explanation}

            ### What you need to do:

            #### Option 1: Install and run pre-commit locally (Recommended)
            ~~~bash
            # Install pre-commit
            pip install pre-commit

            # Install the git hooks
            pre-commit install

            # Run on all files
            pre-commit run --all-files

            # Review the changes and errors
            git diff

            # If there are changes, commit them
            git add .
            git commit -m "fix: resolve pre-commit issues"
            
            # If there are still errors, fix them manually and commit
            git push
            ~~~

            #### Option 2: Quick fix (if you don't want to install pre-commit)
            ~~~bash
            # Format code
            ruff format .

            # Fix linting issues
            ruff check --fix .

            # Run mypy and fix type errors manually
            mypy src

            # Commit and push
            git add .
            git commit -m "fix: resolve linting and type issues"
            git push
            ~~~

            ### Why pre-commit is recommended:
            - ⚡ Catches issues before you push
            - 🔧 Auto-fixes most formatting issues
            - 🛡️ Includes security checks (gitleaks)
            - ✅ Ensures consistency across the team

            ### Need help?
            Check the pre-commit output above for specific errors, or ask a maintainer for assistance.`
            })

      # Case 3: Pre-commit made changes and is still making more changes (infinite loop detection)
      - name: Notify about infinite loop
        if: |
          github.event_name == 'pull_request' && 
          steps.precommit_first.outcome == 'failure' && 
          steps.changes_first.outputs.changed == 'true' && 
          steps.precommit_second.outcome == 'failure' && 
          steps.changes_second.outputs.changed == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🔄 Pre-commit Auto-fix Loop Detected

            **Pre-commit keeps making changes on each run.**

            This usually indicates:
            - Conflicting formatter settings
            - Non-deterministic formatters
            - Pre-commit hooks that need configuration updates

            ### What you need to do:

            1. **Run pre-commit locally multiple times:**
               ~~~bash
               pip install pre-commit
               pre-commit install
               
               # Run multiple times until stable
               pre-commit run --all-files
               pre-commit run --all-files
               pre-commit run --all-files
               ~~~

            2. **Check for conflicting configurations:**
               - Review \`.pre-commit-config.yaml\`
               - Check \`pyproject.toml\` formatter settings
               - Ensure ruff and other tools have compatible configs

            3. **Commit the final state:**
               ~~~bash
               git add .
               git commit -m "fix: resolve pre-commit issues"
               git push
               ~~~

            ### Need help?
            Please ask a maintainer to review the pre-commit configuration.`
            })

      - name: Fail on push events if pre-commit failed
        if: steps.precommit_first.outcome == 'failure' && github.event_name == 'push'
        run: |
          echo "::error::Pre-commit checks failed on push to protected branch."
          echo "::error::Please run 'pre-commit run --all-files' locally and commit the fixes before pushing."
          exit 1

      - name: Fail if pre-commit issues remain
        if: |
          steps.precommit_first.outcome == 'failure' && 
          (steps.changes_first.outputs.changed == 'false' || 
           steps.precommit_second.outcome == 'failure')
        run: |
          echo "::error::Pre-commit checks failed and could not be auto-fixed. Please fix manually."
          exit 1

  type-check:
    name: Type Check (mypy)
    runs-on: ubuntu-latest
    needs: [pre-commit]
    if: |
      always() && 
      (needs.pre-commit.result == 'success' || needs.pre-commit.result == 'skipped')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.13"
          cache: 'pip'

      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('**/pyproject.toml', '**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install dependencies
        run: |
          python -m pip install -U pip uv
          uv sync --no-dev

      - name: Install mypy
        run: uv pip install mypy types-requests

      - name: Run mypy with annotations
        run: |
          echo '::add-matcher::.github/workflows/mypy-matcher.json'
          uv run mypy --show-error-codes --show-column-numbers src
          echo '::remove-matcher owner=mypy::'

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: [pre-commit]
    if: |
      always() && 
      (needs.pre-commit.result == 'success' || needs.pre-commit.result == 'skipped')
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
      fail-fast: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6.2.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Cache uv
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-py${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml', '**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-py${{ matrix.python-version }}-

      - name: Install dependencies
        run: |
          python -m pip install -U pip uv
          uv sync

      - name: Run tests with coverage
        run: |
          uv run pytest tests/ \
            --cov=src \
            --cov-report=term-missing \
            --cov-report=xml \
            --cov-report=html \
            -v

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.13'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

      - name: Archive coverage report
        if: matrix.python-version == '3.13'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: htmlcov/

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [pre-commit]
    if: |
      always() && 
      (needs.pre-commit.result == 'success' || needs.pre-commit.result == 'skipped')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.13"
          cache: 'pip'

      - name: Install security tools
        run: python -m pip install bandit[toml] safety pip-audit

      - name: Run bandit security scan
        run: bandit -r src/ -f json -o bandit-report.json || true

      - name: Run pip-audit
        run: pip-audit --require-hashes --disable-pip || true

      - name: Run safety check
        run: safety check --json || true

      - name: Upload security reports
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: |
            bandit-report.json

  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    needs: [pre-commit]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: moderate

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [pre-commit, type-check, test]
    if: |
      always() && 
      needs.pre-commit.result == 'success' &&
      needs.type-check.result == 'success' &&
      needs.test.result == 'success'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.head_ref || github.ref }}

      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.13"

      - name: Install build tools
        run: python -m pip install build twine

      - name: Build package
        run: python -m build

      - name: Check package
        run: twine check dist/*

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  all-checks-pass:
    name: All Checks Passed
    runs-on: ubuntu-latest
    needs: [pre-commit, type-check, test, security, build]
    if: always()
    steps:
      - name: Check all jobs
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            echo "❌ Some checks failed"
            exit 1
          elif [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "⚠️ Some checks were cancelled"
            exit 1
          else
            echo "✅ All checks passed"
          fi